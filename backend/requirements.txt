# Web framework -- serves the API the desktop (and later mobile/web) app talks to
fastapi>=0.115
uvicorn[standard]>=0.32

# Database: ORM + migrations. SQLite for now; both are chosen specifically
# because they also work against Postgres later with minimal code changes,
# which matters if this ever becomes a hosted multi-user product.
sqlalchemy>=2.0
alembic>=1.13

# Settings loaded from .env, same pattern as discord-checkout-tracker
python-dotenv>=1.0
pydantic-settings>=2.5

# Required by FastAPI to parse UploadFile/multipart form data -- used by
# the spreadsheet-import and backup-restore endpoints.
python-multipart>=0.0.12

# Reads .xlsx files for the spreadsheet-import endpoint. CSV needs no
# extra dependency -- Python's stdlib `csv` module handles that directly.
openpyxl>=3.1

# HTTP client for the live carrier-tracking provider (app/tracking_providers/) --
# the only outbound API call this backend makes to a third party besides IMAP.
httpx>=0.27

# Freezes the backend into a standalone executable for packaging (see
# run_backend.py + backend.spec) -- not needed to run the backend in dev,
# only to build the bundled version Electron spawns in a packaged app.
pyinstaller>=6.0
